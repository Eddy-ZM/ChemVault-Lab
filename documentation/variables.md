# Runtime variables

Durable operation uses `LAB_DB`, `LAB_BUCKET`, and `JWT_SECRET`. AI and OCR providers have independent configuration. Files integration uses `FILES_SERVICE_ORIGIN`, `FILES_LAB_HANDOFF_SECRET`, and `FILES_ARTIFACT_WRITE_SECRET`. Event delivery and analytics use separate secrets and salts.

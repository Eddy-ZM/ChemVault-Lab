# Permissions

JWT sessions scope history and exports to an owner ID. Files import additionally requires a verified email and the Files handoff secret. Artifact writeback uses a separate Files credential. Analytics and lifecycle reads are protected internal operations.

No client-supplied user identifier is trusted as the record owner.

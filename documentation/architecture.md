# Architecture

ChemVault Lab accepts laboratory files, performs deterministic and AI-assisted extraction, persists analysis metadata in D1 and artifacts in R2, publishes completion events, and writes Files-originated derived artifacts back to ChemVault Files.

The browser may run a guest heuristic path, while authenticated server work is durable and associated with a hashed analytics subject.

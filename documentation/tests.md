# Verification

Pull requests run Vitest and a production TypeScript/Vite build. Release checks cover parser fixtures, schema stability, persistence, outbox retry, lifecycle deletion, Files import, artifact writeback, analytics property redaction, and authenticated review decisions.
